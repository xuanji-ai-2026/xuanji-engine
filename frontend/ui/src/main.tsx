import React from 'react'
import ReactDOM from 'react-dom/client'
import XuanjiEngine from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <XuanjiEngine />
  </React.StrictMode>,
)
